# Dev default: serve the dashboard from typed mock data (#144) until the
# backend dashboard endpoint (#146) exists. Production builds REJECT this
# flag (see vite.config.ts) and CI asserts the mock sentinel is absent from
# the bundle.
VITE_USE_MOCK=true
