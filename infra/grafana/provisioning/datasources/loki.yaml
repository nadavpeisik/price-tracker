apiVersion: 1
datasources:
  # The log store (#277). Not the default: the existing dashboards query PriceHuntPostgres.
  - name: PriceHuntLoki
    uid: pricehunt-loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
